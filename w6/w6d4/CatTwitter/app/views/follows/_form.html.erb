<!-- <% if current_user.follows?(user) %>
  <%#= button_to "Unfollow", user_follow_url(user), method: :delete %>
<% else %>
  <%#= button_to "Follow", user_follow_url(user), method: :post %>
<% end %> -->

<button class="follow-toggle" data-initial-follow-state="<%= current_user.follows?(user) ? 'followed' : 'unfollowed' %>" data-user-id="<%= user.id %>">testing</button>
